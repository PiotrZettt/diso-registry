{
  "name": "defiso-certificates-lambda",
  "version": "1.0.0",
  "description": "Certificates Lambda for DeFi ISO Registry",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.844.0",
    "@aws-sdk/lib-dynamodb": "^3.844.0",
    "jsonwebtoken": "^9.0.2",
    "ethers": "^6.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}