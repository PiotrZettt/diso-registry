# API Gateway Configuration
NEXT_PUBLIC_API_URL=https://your-api-gateway-url.amazonaws.com/dev

# AWS Configuration (for deployment only - Lambda uses IAM roles)
AWS_REGION=eu-west-2

# DynamoDB Configuration  
DYNAMODB_TABLE_PREFIX=defiso

# JWT Configuration (required)
JWT_SECRET=your-strong-jwt-secret-key-change-in-production

# Blockchain Configuration (optional - for certificate verification)
ETHERLINK_RPC_URL=https://node.ghostnet.etherlink.com
ETHERLINK_PRIVATE_KEY=your-etherlink-private-key-for-certificate-deployment
ETHERLINK_CONTRACT_ADDRESS=your-deployed-contract-address

# IPFS Configuration (optional - for certificate storage)
PINATA_API_KEY=your-pinata-api-key
PINATA_SECRET_KEY=your-pinata-secret-key

# Development - Local serverless offline
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Note: 
# - Lambda functions use IAM roles for DynamoDB access
# - Blockchain credentials are optional (certificates work without them)
# - IPFS credentials are optional (mock hashes used as fallback)